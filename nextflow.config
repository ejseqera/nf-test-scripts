params {
    outdir          = null
    change_workdir  = 'testing'
}

process {
    container     = "docker.io/library/ubuntu:23.10"
    errorStrategy = "finish"
}

profiles {
    testworkdir {
        workDir = "${params.change_workdir}/work"
    }
}