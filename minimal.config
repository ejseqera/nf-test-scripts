/*
 * Minimal remote config for GitHub inclusion test
 */

params {
    remoteConfigLoaded = true
    foo = "Remote config successfully included from GitHub!"
}

process {
    cpus = 1
    memory = '1 GB'
}
